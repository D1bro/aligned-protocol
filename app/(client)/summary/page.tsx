import { ComingSoon } from "@/components/ui/ComingSoon";

export default function SummaryPage() {
  return (
    <ComingSoon
      title="My Summary"
      blurb="A history view across every completed audit — how your Alignment Score has moved over time."
    />
  );
}
